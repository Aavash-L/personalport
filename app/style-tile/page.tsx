import Link from "next/link";
import { SITE, PROJECTS } from "@/data/portfolio";
export const metadata={title:"Style tile — Aavash Lamichhane",robots:{index:false,follow:false}};
export default function StyleTile(){const p=PROJECTS.find(p=>p.id==='rotgen')!;return <div className="style-tile section"><span className="eyebrow">DESIGN SYSTEM / 01</span><h1>A little curiosity. A lot of care.</h1><p>Aavash Lamichhane · Portfolio direction</p><div className="tile-grid"><div><span className="eyebrow">01 / SIGNATURE</span><img src={SITE.logo} alt="AL Structural monogram" className="tile-logo"/><p>AL Structural · original vector master</p></div><div><span className="eyebrow">02 / PALETTE</span><div className="swatches">{[['Ivory','#FBF8F2'],['Slate','#26384A'],['Blue','#3569A5']].map(([n,c])=><div key={n}><i style={{background:c}}/><p>{n}<br/>{c}</p></div>)}</div></div><div><span className="eyebrow">03 / TYPOGRAPHY</span><h2>Build.<br/>Ship. Repeat.</h2><p>Space Grotesk / Display</p><p>Inter / Clear, readable body copy. Thoughtful products begin with a real problem.</p><span className="minor">INTER / WAYFINDING</span></div><div><span className="eyebrow">04 / INTERACTION</span><div className="contact-actions"><Link className="button primary" href="/#contact">Email Aavash ↗</Link><a className="button" href={SITE.resume}>Resume ↓</a></div><Link className="text-link" href="/#work">Explore the work ↗</Link></div><div><span className="eyebrow">05 / PROJECT</span><a className="tile-project" href={p.url}><span className="minor">{p.metric}</span><h2>{p.name} ↗</h2><p>{p.tagline}</p><div className="stack">{p.stack.join(' / ')}</div></a></div><div><span className="eyebrow">06 / IMAGERY & MOTION</span><img className="tile-portrait" src={SITE.portrait} alt="Aavash, original portrait"/><p>A personal introduction, soft blue accents, and native video chapters. One scroll, one transition, then time to read.</p></div></div></div>}
